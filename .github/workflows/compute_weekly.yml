# ============================================================================
# [1st PIPELINE - PULL MODEL] (legacy)
# Pulls forms_responses.xlsx from an expiring SharePoint share link
# (secrets.FORMS_XLSX_URL -> must be re-shared monthly due to org policy).
# Replacement: .github/workflows/mail_sync.yml (2nd pipeline, mail dead-drop).
# Delete this file once the 2nd pipeline is verified.
# ============================================================================
name: Sync Forms Excel + Compute weekly peer grading

on:
  push:
    branches: [ "main" ]
    paths:
      - "scripts/compute_weekly.py"
      - "requirements.txt"
      - ".github/workflows/compute_weekly.yml"
      - "PeerGrading/Input/week_setup/**"
      - "PeerGrading/Input/students_db/**"

  # [1st pipeline specific] schedule: the 2nd pipeline (mail_sync.yml) has its
  # own Sunday-morning schedule. After cutover, this whole file goes away.
  schedule:
    - cron: "10 23 * * SAT"  # CET: 00:10 Sun
    - cron: "10 22 * * SAT"  # CEST: 00:10 Sun

  workflow_dispatch:
    inputs:
      week:
        description: "Which week_id to compute (e.g., W01). Use ALL for all."
        required: true
        default: "ALL"

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: peergrading-weekly
  cancel-in-progress: false

jobs:
  compute:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # [1st pipeline specific] This entire step is the expiring pull:
      # downloads via the SharePoint share link (secrets.FORMS_XLSX_URL).
      # In the 2nd pipeline it is replaced by scripts/fetch_mail.py (IMAP).
      - name: Download latest forms Excel
        env:
          FORMS_XLSX_URL: ${{ secrets.FORMS_XLSX_URL }}
        run: |
          set -euo pipefail
          mkdir -p PeerGrading/Input/form_exports

          # Ensure download=1 is present
          if [[ "$FORMS_XLSX_URL" == *"download=1"* ]]; then
            DL_URL="$FORMS_XLSX_URL"
          else
            if [[ "$FORMS_XLSX_URL" == *"?"* ]]; then
              DL_URL="${FORMS_XLSX_URL}&download=1"
            else
              DL_URL="${FORMS_XLSX_URL}?download=1"
            fi
          fi

          tmp="PeerGrading/Input/form_exports/_forms_download"
          out="PeerGrading/Input/form_exports/forms_responses.xlsx"
          cookies="/tmp/sp_cookies.txt"

          curl -LfsS --retry 3 --retry-all-errors --retry-delay 2 \
            --compressed \
            -c "$cookies" -b "$cookies" \
            -H "User-Agent: Mozilla/5.0" \
            -H "Accept: */*" \
            "$DL_URL" -o "$tmp"

          python - <<'PY'
          import os, sys, zipfile
          tmp="PeerGrading/Input/form_exports/_forms_download"
          out="PeerGrading/Input/form_exports/forms_responses.xlsx"

          with open(tmp,"rb") as f:
              head=f.read(200)

          if head.startswith(b"PK"):
              os.replace(tmp,out)
              zipfile.ZipFile(out).testzip()
              print("OK: valid xlsx downloaded:", out)
              sys.exit(0)

          print("ERROR: Not an XLSX (first bytes):", head[:120])
          sys.exit(1)
          PY

      - name: Run compute
        run: |
          mkdir -p PeerGrading/Output
          python scripts/compute_weekly.py --week "${{ inputs.week || 'ALL' }}"

      - name: Debug git changes
        id: changes
        run: |
          echo "=== git status (porcelain) ==="
          git status --porcelain || true
          echo "=== git diff --name-only ==="
          git diff --name-only || true

          if [ -n "$(git status --porcelain)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      # [shared logic, duplicated in 2nd pipeline] Same PR mechanism exists in
      # mail_sync.yml but on a different branch (bot/mail-sync-outputs) so the
      # two pipelines never fight over the same bot branch.
      - name: Create PR if changed
        if: steps.changes.outputs.changed == 'true'
        id: cpr
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          base: main
          branch: bot/sync-forms-and-outputs
          delete-branch: true
          title: "Sync latest Forms export + update outputs"
          commit-message: "Sync Forms export and update outputs"
          add-paths: |
            PeerGrading/Input/form_exports/forms_responses.xlsx
            PeerGrading/Output
      
      - name: Enable auto-merge
        if: steps.changes.outputs.changed == 'true'
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          pull-request-number: ${{ steps.cpr.outputs.pull-request-number }}
          merge-method: squash
